apiVersion: v1
kind: Service
metadata:
  name: dashboard-service
  namespace: url-shortener
  labels:
    app.kubernetes.io/name: dashboard
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: dashboard
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081
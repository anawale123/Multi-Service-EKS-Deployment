apiVersion: v1
kind: Service
metadata:
  name: api-service
spec:
  cluster:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: api
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080

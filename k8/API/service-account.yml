apiVersion: v1 
kind: ServiceAccount
metadata:
  name: api-sa 
  namespace: url-shortener
  annotations:
   eks.amazonaws.com/role-arn: arn:aws:iam::703844615264:role/api-irsa-dev-${var.environment}

